---
# Ansible collections required by the playbooks.
# Install once with: ansible-galaxy collection install -r ansible/requirements.yml

collections:
  - name: community.general
    version: ">=8.0.0"
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
